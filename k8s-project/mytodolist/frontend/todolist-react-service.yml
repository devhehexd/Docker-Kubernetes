apiVersion: v1
kind: Service
metadata:
  name: todolist-service-react
spec:
  selector:
    app: react  # Deployment의 labels와 일치해야 합니다.
  ports:
    - protocol: TCP
      port: 80           # 클러스터 내부에서 사용하는 포트
      targetPort: 80    # Nginx 컨테이너의 포트
  type: LoadBalancer