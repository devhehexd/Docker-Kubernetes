apiVersion: v1
kind: Service
metadata:
  name: todolist-spring-service
spec:
  selector:
    app: spring
  ports:
    - protocol: TCP
      port: 8081       # 클러스터에서 사용할 포트
      targetPort: 8081 # Spring Boot 컨테이너에서 사용할 포트
  type: ClusterIP